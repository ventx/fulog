The local file endpoint will work as a file mirror. It will just perform the action done on the 
virtual files on a local file. 

### Options

#### root
**root** defines the location prefix of the files to store
